// main.js
// Placeholder main script — keep lightweight to avoid interfering with other scripts

document.addEventListener('DOMContentLoaded', () => {
  // Currently no global behavior needed here. This file exists so <script src="./js/main.js"></script>
  // does not 404 and block other scripts.
});
